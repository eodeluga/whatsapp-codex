# Deployment substitutions only. Keep OpenWA API credentials and webhook
# signing secrets in $CODEX_HOME/config.toml, never in this file.
CODEX_HOME=/absolute/path/to/.codex
UID=1000
GID=1000
OPENWA_IMAGE=rmyndharis/openwa:1.0.0
CODEX_WHATSAPP_BRIDGE_IMAGE=codex-whatsapp-bridge:local
